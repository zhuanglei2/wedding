# V10.82 · 满版平整请柬首帧预览

## 输出

- `first-frame.png`：1400×3282 完整静态合成图，保持原请柬比例，无额外边条，不裁切、不预先翘纸。
- `contact-detail.png`：右上区域检查用裁切，不是完整视频首帧。
- `groom.png`、`bride.png`：从 V10.81 透明图中分离出的完整角色，按比例缩小供合成使用；没有重绘。
- `verification.json`：原件哈希、摆放坐标与像素检查。

## 布局与验收

原请柬使用 `versions/v10.71-collar-and-motion/cover.webp`。男孩放在右上侧，手掌接近右边界，保留 8 原图像素的安全距离；女孩放在较低、偏左的花纹区域。没有遮挡中央喜字、正文或真人照片。人物没有切掉帽子、手掌、鞋或裙摆。

像素检查：人物叠层之外 0 像素变化；y≥730 的正文、照片、姓名及地址区域 0 像素变化；最左与最右边界 0 像素变化。源请柬与 V10.81 原件哈希保持不变。

这是手掌贴面前的静态姿态预览，不是已经验证的抓握，也不是视频。手腕抬起后能否与掀起的卡面保持接触仍需测试。原请柬长宽比不是 9:16，不能在上传时自动补边或变形；本轮没有上传，也未核实平台是否原比例支持该首帧。

## 后续动作约束

首帧平整满版 → 手腕与肩膀带起右侧封面 → 建立边缘接触 → 整张厚卡沿最左折痕向左完全翻开。女孩注视后稍晚跟随，不同时抓纸；服装跟随有轻微滞后。米色边条、初始翘角、初始露底、封面缩小都不允许。

仅使用已确认的本地素材合成流程，沿用 HyperFrames 现有项目定向编辑范围，没有引入新生成服务或渲染框架。未运行浏览器预览；通过本地输出图片目视检查。未消耗 PixVerse 积分、未部署、未修改首页入口。

复现：Node 执行 `compose.cjs`（使用已安装 bundled sharp）。
