# V10.87 · 四秒动作提交前准备

状态：**HOLD，不能提交生成**。这是首帧候选和检查材料，不是新动画，不是上线版本。

## 已做

- 只用 V10.81 两个完整角色透明素材，按不透明连通域分开；不重新画脸、颈部或衣服。输出保留独立原像素角色 PNG，参考排版时等比缩放。
- 900×1600（9:16）技术载体。空白红卡 x=130..770、y=50..1550，比例约等于正式长请柬；外侧灰色是生成工作区，不会加到正式网页上。
- 男孩完整高 400px，右侧毛绒手掌贴近卡边；女孩下方独立站位，垂直间隙 80px。不要求毛绒掌生成五根手指。
- 留出向左 200px 的设计路径，以及帽子、手和裙摆的画面安全距离。`verification.json` 是此规定路径的几何检查，不代表生成结果。
- `PROMPT.txt` 为新四秒动作稿，无真人照、姓名、地址，且不要求这四秒完整开卡或退场。
- 网页尚未改成此草稿，未上传新首帧、未切换参数、未生成、未消费积分。

## 导演节奏（不是模型逐帧承诺）

| 时间 | 男孩 | 女孩 / 纸张 |
|---|---|---|
| 0–0.6s | 看接触点、毛绒掌贴稳 | 女孩看男孩手；纸先不走 |
| 0.6–1.1s | 弯肘、肩和身体后倾，表现发力 | 女孩约晚 0.25s 跟随 |
| 1.1–3.2s | 保持同一掌边接触，向左短拉 | 左轴固定，厚卡浅弯；女孩下方随行，不抓纸 |
| 3.2–4s | 保持抓点，减速停住，不出框 | 衣摆头纱轻微滞后收稳；留给续段 |

原讨论中的“向左拉”在本轮仅是局部开卡。若用户改为四秒完全打开，需要重新评估路径；不能偷偷把小幅试拉说成完整转场。

## 第二段接续合同

- 先验收第一段，再从**实际输出**选择稳定末帧；不能提前用静态摆拍假定它是成片尾帧。
- 下一段首动作必须是男孩从纸边松手，网页厚卡继续打开，再等待/会合/飞走。否则会出现手还粘着纸就飞走的问题。
- 第一段末帧与第二段首帧的画幅、人物位置、大小、服饰、朝向、背景一致；至少检查前后各 6 帧，不能用整体交叉淡化隐藏双人重影。
- 每段都要独立做人物遮罩和接触检查。网页后加拖尾不需要让 AI 视频画星星。

## 不能跳过的验证门槛

1. **真实视频文件未拿到。** 2026-09-09，对已生成 V10.78 的浏览器素材导出失败（Failed to fetch）；官方“下载→有水印”入口也未返回下载事件（10 秒）。没有绕过浏览器/网络限制。对应既有结果： https://app.pixverse.ai/video/423428644819756 。需用户提供下载好的 MP4 后才能完成像素级测试。
2. **无透明视频层。** 原素材是透明 PNG，不代表生成 MP4 也有透明背景。空白红卡、背景、人物的分离，以及白色领口/头纱边缘，需要实际视频逐帧验收。不能用静态 PNG 抠图成功冒充视频成功。
3. **抓点不等于纸张网格。** 追踪到一个手掌点，仍不能唯一恢复整张纸的三维曲面。应使用受控网页厚卡，并验收其自由边和手在画面的接触；若生成轨迹无法拟合，判失败，不改动真实照片补救。
4. **现有旧片不能证明新提示词成功。** V10.78 的小人物、双人抓纸和软纸变形，仅用于检查处理链路，不做正式素材。
5. **手机衔接未通过。** 原请柬比视口长；需锁定触发时视口及滚动位置，核对同一坐标映射、预加载和播放状态，防止开始跳尺寸或露工作区。
6. **积分门槛。** 上次读到余额 30。正式提交前须新检查 V6、540P、4 秒、1 条、音频关、多镜头关与页面实际费用；不沿用旧的 6 秒/36 点草稿。本次并未核实切到4秒后的实时价。

## 本轮检查证据

- 本地运行 `prepare.cjs` 成功：保留两个完整连通域；设计路径最小载体边距 130.06px，角色垂直间隔 80px；指定静态掌边到卡边距离 1.47px。源 SHA-256 与 V10.83 记录一致。
- 实际查看 `layout-check.png`：帽子、完整白领、白袖、花束、婚纱裙摆在静态首帧内可见。图中文字和抓点仅供检查。
- `git diff --check` 通过。
- 本地 HTML 的浏览器打开被 URL 安全策略阻止，未改用其他路径绕过；不声称该检查页已完成浏览器/手机视觉验收。PNG 已通过本地图片查看检查。
- 没有可用于新片的动态 alpha/追踪测量。因此本轮结论仍为 HOLD，需要已有四秒样片 MP4 文件才能继续实测。

## 验收阈值（未来执行，当前未通过）

- 全片只有两角色，帽子、领口、裙摆和手不出框、不合并，脸和服装不变形。
- 逐帧检查，不只看首尾：深底、浅底两种合成背景下，衣领无洞，白纱无明显吃边、色边或闪烁；保留源水印，不购买去水印功能。
- 接触期间每帧记录掌边和纸自由边；手机等效宽390px时，目标误差≤2px且无瞬间跳动。阈值只是验收标准，尚无测量结果。
- 第一段尾部连续至少0.4秒可用于接续；不出现退场、换姿态跳变。
- 若旧片下载/抠像/手边同步任一不通：继续HOLD，不消耗24点碰运气。

## 文件

- `first-frame-candidate.png`：无标注参考候选，尚未批准上传。
- `layout-check.png`：带抓点/安全范围的检查图，禁止作为生成首帧上传。
- `index.html`：离线检查页，无生成/上传接口。
- `prepare.cjs`：本地重复生成静态排版及几何检查。
- `verification.json`：分开标记已通过和未通过的门槛。

仅本目录为本次变更；源文件、原请柬、love.html和OSS均未改动。
