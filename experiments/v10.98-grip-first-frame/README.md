# V10.98 — 手套包边首帧候选

本版通过内置 image_gen 编辑 V10.97 的源视频第24帧，生成一次静态候选图。
没有调用 PixVerse，没有生成视频，没有部署或更改现有网页。
不能将“未消耗 PixVerse 积分”描述成所有服务都不消耗任何额度。

## 文件

- `first-frame-candidate.png`：项目内保存的候选，954×1649。
- `IMAGE-PROMPT.txt`：实际提交给内置图像编辑工具的完整提示词。
- `VIDEO-PROMPT-DRAFT.txt`：适配该候选的下一次单动作视频草案，尚未提交。

输入：`../v10.97-motion-assets/reference-source-f024.png`（592×1024）。
本版未上传真实请帖、婚纱照、姓名或地址。生成图只有空白红卡与两个卡通角色。

## 检查结果与限制

- 保持两名小角色，男孩位于右侧纸边、女孩在下方偏左；没有增加角色或写实人手。
- 目标手仍是圆手套；可见前后两块圆润的垫状形体跨在卡片右边缘附近，比单纯圆球触边更可读。
- 白袖口、另一只手、黑色礼服和鞋子可见；女孩衣领、头纱、花束及裙身完整可见。
- 卡面基本闭合、边缘平直，未加入白色翻面、绳子或把手。
- 手肘变化依旧克制，静帧只能说明握持关系候选，不能证明后续肩肘发力自然或抓点不滑动。
- 整体经生成模型重渲染：纹理、光影及人物细节不是输入逐像素保留；输出尺寸也变化了。
  不能宣称女孩完全没被改动，也不能直接沿用 V10.96 的像素坐标遮罩。
- 上述为整图人工视觉检查，不是身份相似度或每一像素边界认证。

## 视频制作门槛

使用此图前仍需审核角色形象、大小和手套握持是否符合预期。
下一次只做“握持—预备发力—单次拉动—保持”，不加入松手、等待、牵手、飞走或拖尾。
生成前重新检查 PixVerse 的可选时长、费用与余额，需明确授权该次消耗；不自动重试。
静帧改善不等于视频一定成功，不等于真实请柬的动态合成已完成。

来源输出保留在原生成目录；项目副本独立版本保存，V10.97 与旧网页不覆盖。
