# V10.124 · 更清晰的快门闪光

基于 V10.123（a9449ca）。仅加强拍立得快门闪光，未部署、未推送。

- 全屏曝光由 48% 提高至 78%，淡暖色改为纯白；1660ms 达峰，保持 45ms，2030ms 完全退去。
- 相机光晕直径从画面宽度的 30% 扩至 42%，保持同一镜头中心，亮起稍早于全屏曝光。
- 沿用同一动画时间轴：只闪一次，不循环、不添加独立定时器；仅变化透明度和缩放，不改变页面尺寸。
- 照片原件、姓名日期、纸纹、角色动作、打印/放大/文字显现时序不变。
- 保留减少动态效果和打印模式禁用闪光，以及滚动中止/页面隐藏后的清理。
- 按 HyperFrames 动效技能的触发同步、快速亮起/柔和衰减原则处理，保留现有静态 HTML 实现，不迁移框架。

检查：曝光回归、相机故事、纸纹、文案、加载和现有动画测试。尚未进行微信真机或浏览器视觉验收。

回退：../v10.123-full-paper-texture/index.html
