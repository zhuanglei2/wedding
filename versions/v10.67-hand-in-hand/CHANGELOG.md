# V10.67 · 悬停邀请、牵手离场

- 男角色等待时增加轻微上下起伏、倾身、伸右手邀请、空闲手摆动和抬腿；女角色靠近后伸左手回应。
- 在 0.79 进度牵住，0.82 进度再共同起飞，约 0.14 秒握持停顿；完整动画 4.8 秒。
- 两人的牵手位置由同一个接触点约束，包含身体旋转和缩放，飞行时不会因倾斜导致双手分开。
- 飞行中允许不同的身体倾角、自由手与腿部摆动，替换之前固定身体中心间距的平移动作。
- 使用 HyperFrames 抓取同步与按压回应原则；保留现有原生网页、角色分层素材，不换图、不拉伸肢体、不新增人物。
- 保留 V10.66 完整自动播放、滚动恢复、减少动态效果、后台与转屏收尾机制，以及 24 段纸面和底部隐藏素材修复。
- 新增独立计算的渲染手部接触断言，覆盖牵手至离场全程、四种屏宽和各阶段位置连续性；保留原运行测试并按新时长验证完成。
- 检查了实际角色绘制器的六个姿势时间点；未进行浏览器交互/真机视觉验收。素材、纸面和播放控制文件字节校验通过。
- V10.66 保留，未部署 OSS、未推送 GitHub。

## 继承 V10.66

- 男角色在 0.71 进度到达会合位置，悬停等待至 0.80；女角色在 0.79 到达，二人在 0.80 使用相同轨迹增量同时起飞，保留星光拖尾。
- 两个人物宽度相对 V10.65 缩小 15%，共用缩放上限与手机端比例；素材、衣着、五官均不修改。
- 翻纸从 12 段细化为 24 段连续曲面，增强渐进弯曲并减淡阴影。纸背使用整张连续渐变，修复每片重复渐变产生的条板感；取消逐片 brightness 滤镜。
- 将逐滚动位置采样改为点击/滚动触发的独立 4.5 秒播放；手指停下不再暂停人物，触碰也不再取消播放。仅播放期间拦截单指滚动，结束恢复原生滚动；双指缩放保留，Escape 可直接结束。
- 播放期间冻结地址栏伸缩造成的高度变化；横竖屏变化、切到后台会收尾。资源错误、减少动态效果回到静态导航；返回首屏可以再次触发。
- 按 HyperFrames 的抓取同步、蓄力与分段动作原则重排；按 Sites 技能保持既有静态网站和局部改动范围。未引入新框架、未生成新图片。
- test-page-turn.cjs 覆盖同步离场、等待、缩放、接触点、滚动中继续播放、地址栏/方向变化、后台、资源错误、减少动态效果及恢复滚动。test-paper-surface.cjs 覆盖连续纸面和统一纸背渐变；其他素材与拖尾测试保留。
- 使用实际角色绘制器检查离场关键时刻的素材轨迹图；未进行浏览器交互或手机真机视觉验收，不宣称帧率已验证。
- V10.65 完整保留，线上版本不变；仅本地保存和提交，不部署、不推送。

## 继承 V10.65

- 修复全局 img 的 display:block 覆盖 hidden 默认样式，导致动画素材图集在 main 与 footer 之间占位的问题。
- 针对 .rig-source[hidden] 显式设为 display:none!important，覆盖正常、加载中、减少动态效果和打印状态；Canvas 仍可读取原图。
- V10.64 动画、请柬内容及照片均不改动；仅新增隐藏样式、版本标记及验证。
- 已保留 V10.64；仅本地提交，未部署、未推送。采用 Sites 最小范围修复原则；静态/模拟测试验证，未做浏览器视觉验收。

## 继承 V10.64

- 基于 V10.63，修正人物飞入大部分发生在画面外、松手前已离开左边界的问题。
- 改为错峰弧线飞入、握持蓄力、后仰拉纸、画面内松手、独立向右上方飞走。
- 缩短完整动作至 3.9 秒；纸张使用连续角速度曲线，在松手后继续翻页。
- 保留向外掀起方向、星光拖尾、关节动作、首屏点击以及减少动态效果的降级导航。
- 依据 HyperFrames 动画技能处理抓取同步与蓄力释放；保持既有静态网页架构。
- 测试：test-page-turn.cjs、test-paper-surface.cjs、test-star-trail.cjs、validate.py；新增松手可见性、释放轨迹连续性和离手后纸张继续运动的断言。
- test-flight-poses.cjs 用实际角色绘制器输出六个时间点的独立素材轨迹图；未做浏览器交互或真机验收。
- 原请柬、角色贴图及真人照片字节不变。V10.63 完整保留，可回撤；未部署 OSS、未推送 GitHub。
