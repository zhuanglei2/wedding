# 第四页素材

照片来自用户本轮提供的两个本地文件，详见 asset-report.json；保留源文件不写入、不裁切、不重绘。仅生成用于网页的多尺寸编码副本。

宴席线稿复用 ../v10.129-red-frame-collage/banquet.png；蝴蝶结复用同版既有 SVG。原线稿生成提示见该版本 ASSETS.md；本次未调用图像生成。

网页装饰副本缩至 880×660，WebP 质量 90，保留完整透明通道。只作分辨率与编码优化，不改线稿构图。

文字使用现有 Noto Serif TC 的最小子集，标题、姓名和实际信息以 HTML 排版。参考图只作为样式依据，未复制他人姓名、错误日期、平台界面或水印。
