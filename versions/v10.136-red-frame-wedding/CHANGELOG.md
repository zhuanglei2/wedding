# V10.136 · 第四页红框婚礼拼贴

- 基于 V10.135，只替换第四页草坪内容；前三页、翻页及相机动画、登记页入口不变。
- 参考奶油白底、错落红框照片、黑色蝴蝶结、酒红邀请文案及宴席线描；更新为庄磊、吴郁、2026.10.06，晚宴及实际酒店地址。
- 用户两张原照未覆盖、未生成或修改人物；派生图片仅缩放、编码，右图在 CSS 中裁切。
- 三档 WebP、JPEG 兼容回退、延迟到第四页附近才加载，不加入首屏预加载。局部字库替代全量字体，并补齐上版标题字库缺失的依赖。
- 沿用已有蝴蝶结 SVG 和已生成宴席线稿，没有新增图片生成调用或动画库。
- 独立可回退版本，未部署、未推送。

## 验证与传输量

- 静态与实际媒体控制器检查通过：前三页和收尾内容保持一致，35 个本地依赖有效，第四页顺序正确，延迟加载等待首屏就绪并接近视口，兼容 JPEG 与无 IntersectionObserver 的回退。
- 两张原照共 10,349,061 字节，原文件 SHA256 发布前后保持不变。800px WebP 档合计 247,832 字节；480px 档合计 102,412 字节；1200px 档合计 481,536 字节。浏览器按显示宽度和屏幕像素比选择一档，不会同时下载全部尺寸。
- 宴席装饰 243,790 字节；局部宋体 30,592 字节。本页无新动画循环或第三方库。常用 800px 照片、装饰与字库合计约 522KB；不包含其他页面资源，不是实测加载时间。
- 未进行浏览器像素或微信真机复核。
