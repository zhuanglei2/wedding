# V10.112 · 星星人拍立得故事

- 完整页面：约3.8秒翻页，星星人在会合处等待，不先飞出屏幕；翻页完成后将同一位置和姿态传给约5.4秒的拍立得段。
- 拍立得段：减速飞到快门→眼神跟随/短转向→左手接触快门并按下→一次200毫秒、最高16%不透明度的暖白闪光→两秒吐出原始照片。
- 打印启动后两人回身会合，双手共用一个接触点，沿弧线牵手离场并保留星星拖尾。
- 照片在该段3.8秒时完全吐出；3.95秒才开始显现Record of happiness / 记录幸福；4.35秒才开始显现姓名日期，5.2秒完成。绝不在照片未出完时提前显示。
- imagegen只生成去除相片框与底部文字的干净底稿。原照不交给AI、不重画人物。原始标题/框/底部字由V10.111原底稿分层取用，动画完成后恢复原底稿完整视觉。
- 新生成的侧身姿态图两次均返回不含真实alpha的RGB方格底，未接入网页。角色采用现有完整衣领的分层骨骼素材与轻度转向，不是新侧身视频或完整三维转身。未使用PixVerse积分。
- 网站采用原有原生脚本，按HyperFrames单时间线/可采样动作原则实现。照片、说明和姓名为独立层；Sites局部集成，不改变第一屏照片和后续章节。
- 页面切后台、尺寸变化、Escape、Tab和多指缩放均有显示完整内容的中断出口；减少动态效果时显示静态请柬，无闪光；素材异常不永久隐藏照片文字。
- 第二页预览自动演示一次并可点“重播”；完整流程需从index.html点击第一页开始。重复点击不会叠加计时器。
- 验证：test-camera-story.cjs、test-runtime.cjs、test-cover-handoff.cjs均通过；包含24组尺寸采样、15种第一页点击位置、手部接触、严格文字时序及生命周期测试。未声称浏览器或微信真机验收。
- 仅本地新版本；V10.111及旧素材保留，不部署、不推送。生成记录见IMAGE-PROMPTS.md。
