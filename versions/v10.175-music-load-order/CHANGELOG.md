# V10.175 · 视觉优先加载音乐

- 基于 V10.174，首屏原图、最后一页闻涛厅及 16:58、所有页面和动画样式不变。
- 音频地址改为 data-src，初始 HTML 不提供可直接下载的 MP3 src。
- 点开请柬仅用内嵌的 20 毫秒无声音频尝试激活同一个播放器，不下载歌曲。
- 已打开请柬且 camera-assets-ready（首屏、第二页照片和角色素材已解码）后，才挂载原 MP3 并缓冲；直接进入相机动画也能触发。
- 未点开请柬时不预下载歌曲；静态/减弱动态效果的页面，在照片可见且显现完成后按需加载。
- 照片显现后仍从 18 秒播放，保留一次播放、暂停、进度、后台恢复及自动播放被限制时的手动按钮。
- 音乐加载不参与翻页或打印动画的完成条件；手势准备有 350 毫秒兜底，迟到的回调不得重置播放进度。
- 不改音质、不复制音乐文件、不部署。自动播放仍受浏览器规则约束，微信真机需部署后验证。
