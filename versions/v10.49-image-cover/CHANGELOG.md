# V10.49 · 定稿图片嵌入首屏

- 将已确认的 V10.48 高清定稿嵌入 HTML 首屏，1400 × 3032 等比例显示，不裁切、不拉伸、不重新排字。
- 优先加载无损 WebP，PNG 作为兼容回退，像素与定稿保持一致。
- 首图优先加载，预留宽高，避免布局跳动；保留可访问的文字版姓名、日期、晚宴、酒店及详细地址。
- 沿用 V10.45 的后续三组婚纱照、文案、赴约登记及样式，不新增后续设计。
- 新版独立保存；发布目标仅为香港 OSS 的 love.html，旧页面另存预览目录备份，不改线上 index.html。
