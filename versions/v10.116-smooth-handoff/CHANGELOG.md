# V10.116 · 底部留白与动画交接

## 定位与修正

- 第二页海报按2:3绘制，但外层仍设为至少一屏高。390×844的视口中，海报585px、高度差259px成为多余空白。取消外层最小屏高；保留原排版与比例。
- 下一章节原有向上48px的深色渐变，在去掉多余占位后会遮到日期，已同步取消这一叠加，不改后续章节内容。
- 原首段在约2472ms到达0.79进度后，把角色姿态固定到3800ms。改为最后一次松手（0.64）前完全不变，之后以连续曲线汇合，3800ms才到达原交接姿态；没有加快/反向拉纸。
- 第二段接收首段的时间戳，下一帧继续累计时间，不再重新等待起始帧。
- 第一段准备时记录第二页几何，交接直接使用，不在移除固定定位、滚动之后立即重新测量页面。
- 第二段在资源就绪阶段分配画布缓冲；缓存样式值，未变化的属性不反复写入。

## 保留

原照、欢迎语、姓名日期、打印与放大尺寸、闪光强度、文字延后时序均不改。无重播控件。V10.115及之前版本保留；未部署OSS、未推送GitHub。

用户追加「纸张拉长」存在三个可能目标（背景纸、相纸、红色请柬），已请求确认；本版不擅自拉伸。

## 验证

- `test-handoff-math.cjs`：松手前完全一致、交接曲线单调与速度衔接、24组页面尺寸无额外屏高。
- `test-cover-handoff.cjs`：真实首段控制器回归，验证末段仍移动、末帧一次交接、时间戳与坐标、原位点击/滚动不中断。
- `test-runtime.cjs`：真实第二段控制器VM检查预分配画布、不在交接测量DOM、时间连续、未变化样式不重复写；终态及中断兜底。
- `test-camera-story.cjs`：打印/放大/文案次序、原照校验、资源、正式入口与无重播。

以上四组及脚本语法检查通过。没有执行浏览器视觉或微信真机帧率测试；消除已定位停顿不等于对所有设备帧率作保证。
