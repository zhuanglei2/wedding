# V10.104 · 首屏渲染依赖优化

本地新版本，未部署、未推送；线上仍为 V10.103。回退：../v10.103-led-departure/index.html。

全部动画脚本、CSS内容、图像素材与V10.103逐字节一致。仅将三份CSS内联、延后非首屏字体请求；首屏原图不缩小、不重编码。
validate.py可逆还原内联及字体加载改动后，将HTML与V10.103完整比对；test-cover-first.cjs验证字体在图片解码后、两个帧回调后才加入，且不重复请求。
CSS原文件保留为内联内容的校验来源；如以后修改CSS，请同步index.html内对应data-inline-source样式块，否则验证会失败。
12项JS回归及素材验证通过；本地预览无法衡量微信网络下的首次出图速度，部署后仍需用户确认。

## 继承 V10.103 · 有先后的牵手离场

本地新版本；未部署、未推送，线上仍为 V10.101。
回退：../v10.102-in-place-turn/index.html。

仅在握手完成后的 .805 进度开始改动作；男孩先前倾并改变牵手臂角度，女孩晚 .024 进度回应。
共享牵手轨迹、总时长、纸张和入场全部不变。先姿态再位移、先躯干再下摆，一次收腿伸展代替循环摆腿；手接触校正在全部躯干动作之后进行。
原位翻页的 page-turn.js / page-turn.css 逐字节继承 V10.102。全部图像和 character-rig.js 也不变，保留衣领连贯性，不声称有真实三维转头。

验证：11项 test-*.cjs 与 validate.py 通过。新增 test-led-departure.cjs 覆盖动作延迟、下摆跟随、连续性、手部连接和入场不变。
render-departure-review.cjs 可输出新旧关键帧对照与171帧实际画布渲染；已看离线姿态对照，未做浏览器/微信真机验收。

## 继承 V10.102 · 原位开始翻页

本地新版本，未部署、未推送。线上仍为 V10.101。

长请柬在390px宽时约914px高，保留全文与字号；不为一屏展示裁切或整体缩小。
点击后读取当前纸张矩形，固定相同 top/left/width；纸张透视中心重新定位到视口60%高度，与人物抓握使用同一坐标。
播放不再执行250ms底部对齐或逐帧滚动。原轨道保留布局，纸张消失后才一次性切换到第二页位置。
新增15种尺寸/阅读位置模拟断言，以及残余滚动和退出清理测试。浏览器读取受限，尚未完成微信真机视觉验收。

回退：../v10.101-fluid-flight-loading/index.html。

## 继承 V10.101 · 飞行节奏与资源准备

以下记录为 V10.101 制作阶段；V10.101 后续已部署，见仓库部署记录。

主要改动：飞入路径与伸手姿态匹配速度，分开入场路线；共享曲线牵手离场，离场约1.98秒、总时长6.8秒；动作后随衣摆，领口/头部保持原结构。没有AI视频。

资源：eye-lids.png只存原表情图的四片闭眼区域，rig-atlas.webp无损；记录见ASSET-OPTIMIZATION.json及.media/manifest.jsonl。首屏和照片原文件字节不变。

检查：test-fluid-flight.cjs验证速度/轨迹；test-page-turn.cjs验证首图优先、贴图完成与解码后再启用动画；其余测试继承并使用新的实际贴图。render-flight-review.cjs生成离线实际Canvas帧到临时目录；不是网页或微信性能测量。

回退：../v10.100-refined-character-scale/index.html。

## 继承 V10.100 · 角色比例与弱透视

基于 V10.71 完整网页，未接入 AI 视频。角色尺寸缩至此前 80%，星点、光晕和投影减轻；纸面与抓握坐标共用弱透视，降低拉纸中段的右侧出框。时长和动作阶段未改。

新增 test-character-scale.cjs 输出 `/private/tmp/wedding-v10100-size-comparison.png`：真实 Canvas 角色绘制器在 390px 宽度下的拉纸与牵手姿势对照；这是离线姿势图，不是浏览器截图。

9 个 JavaScript 检查与 validate.py 通过。浏览器 file:// 读取被策略拦截，手机真机和完整视觉播放未验收。原有二维素材的自然度限制仍存在。

回退打开 `../v10.71-collar-and-motion/index.html`。未部署 OSS、未推送 GitHub。

## 继承 V10.71 · 恢复完整领口与连贯动作

基于 V10.70 新建，旧版本原样保留。仅本地，不部署 OSS、不推送远端、不改首页入口。

## 修正方案

- 撤下不匹配的替换头像和肩颈拼接。原图连续的头部、脖子、项链、领口、头纱、花束和上衣保持同一层，绕腰部小幅倾斜；不再切开脖子或添加黄色补片。
- 撤回上一版的侧脸素材切换：当前是完整上半身带动头部的二维动画，不是真正三维转头。
- 眼白、虹膜和肤色来自原图，虹膜连续跟随纸边、对方和飞离方向。错峰眨眼，眼神先变化，上半身随后回应。
- 减少反复摆手踢腿，集中表现抓纸、等待、伸手、牵手和共同离开。松手曲线继承拉纸速度，再减速到等待位置。
- 上半身倾斜纳入手部接触坐标；裙摆和衣摆在腰下轻微延迟跟随。
- 保留 5.6 秒完整播放、男孩等待女孩、牵手停留、星星拖尾、纸张、点击/滑动触发与异常收尾。

## 素材边界

未生成或改写任何图片。rig-atlas.png、请柬和真人照片字节不变。
head-expressions.png 仅取极小的闭眼弧线片段，不绘制其中任何完整头像。head-prompt.md 是 V10.70 历史生成记录；head-masks.js 和测量脚本仅作历史参考，不由网页加载。

## 验证

- test-collar-gaze.cjs 对中立姿态的领口、项链和花束区域作原图像素比较，检查两人虹膜移动及闭眼弧线，输出 /private/tmp/wedding-v1071-collar-check.png。
- test-expressions.cjs 使用真实孤立角色绘制器检查确定性、眼神和身体/衣物先后关系，输出表情及手机尺寸姿势对照图。
- test-page-turn.cjs 独立计算上半身变换后的手部接触，回归不同屏宽下的牵手、布局、滚动播放、资源错误、转屏、后台和减少动态效果。
- 保留纸面、粒子、角色与飞行姿势测试；validate.py 检查既有图片与请柬内容未改动。
- 未进行浏览器交互或手机真机验收，孤立姿势检查不能证明手机完整动画流畅度。

本地预览：index.html，点击请柬或下滑。回退打开 ../v10.70-living-characters/index.html。
