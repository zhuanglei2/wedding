# V10.127 · 边缘隔离与姓名字号

基于 V10.126；仅本地保存，未部署 OSS、未推送 GitHub。

- 相纸分片改为 instant-film 专用类，避免覆盖首屏翻页的 paper-strip 样式。
- 在不参与 sticky 布局的 reference-art 内裁剪放大层；根级只使用 overflow-x:clip，不把 body 改为滚动容器。
- 触屏落页不再显示整页蓝框；键盘进入保留内嵌焦点提示。结束时仅在未放大视口清理横向偏移，不动纵向位置。
- 原始手写姓名独立放大 16%，日期及分隔点仍使用原位原尺寸窗口。照片、相纸比例、纸张页高和动画时序不变。
- 复用现有优化资源，不生成图片、不请求新字体。

验证：本目录 10 项自动检查全部通过，覆盖分片样式隔离、触屏/键盘焦点、横向偏移恢复、姓名与日期边界及原有动画回归；尚未进行微信真机或浏览器像素复核。
