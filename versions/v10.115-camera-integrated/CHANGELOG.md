# V10.115 · 完整请柬整合

用户认可V10.114效果，要求接入完整页面并去掉重播按钮。

- 正式入口为本目录 `index.html`，从红色请柬开始；点击首屏后，翻页接棒拍立得动作。
- 沿用已确认的闪光、打印、相片放大、装饰淡出及文字渐显，不修改视觉时序或原照。
- V10.114完整HTML原本没有重播按钮；本版进一步移除其引用样式和控制器中的重播入口、独立预览自动播放逻辑，正式页面没有预览控制条。
- 同一页面访问内不会通过重播参数再次触发动画；自然滚动/直接进入第二页的既有入口及减少动态效果兜底保留。
- 第一屏图像、后续章节、登记入口不变；复用既有图片、动作数学及翻页程序，不增加图片资源。
- V10.114及其带重播的实验预览保留，便于回撤。

三组离线检查通过：动作时序/原照完整性与无重播断言、实际控制器生命周期、完整翻页交接回归。没有执行浏览器视觉或微信真机测试。

仅本地版本，未部署OSS、未推送GitHub，根目录首页和guest.html未修改。
