# V10.114 · 照片与插画分层修正

基于V10.113；修复用户看到的红色相机边缘和蓝色星星被带入放大效果的问题。旧版本保留。

## 修正

- `.camera-print-sheet` 不再使用完整相机插画作背景，不再依赖插画裁切来保证内容干净。
- 移动相片中仅包含一张 `couple-original.jpg`；外缘保留简单细蓝线，与页面配色呼应。照片不生成、不修改源文件。
- 相机淡出纸纹覆盖区域从42.6%向上扩展至38%，并采用右上伸出的轮廓：遮住蓝星，但避开标题红色下伸笔画。
- 纸纹只复用现有清底图片的底部空白区域；上方欢迎语、底部文字、姓名日期不改。
- V10.113的单次增强闪光、打印→等比放大→文案出现时序、牵手离场均不变。

## 验证

- 三组离线检查通过：动作数学、实际控制器生命周期、完整翻页交接回归。
- 新增断言逐个检查预览与完整页面的移动相片：只包含原照，不含相机插画或清底图；移动层CSS无背景图。
- 原照SHA256不变：`39e6248100d2e1b8eb3ca2cc39af61c0799f76d3211192e31fb3efa7a072040a`。
- 用户要求查看问题时，尝试读取现有本地浏览器页，被Browser Use URL policy阻止；没有绕过限制，没有宣称浏览器/微信视觉验收。

本地预览：`preview.html`；完整请柬：`index.html`。未部署、未推送。
